package net.exachixkitsune.magicalmetals.blocks;

import net.minecraft.block.AbstractBlock;
import net.minecraft.block.Block;

// Essentially an empty block that is a special type for linking to magic-using objects.
public class MagicalFocusBlock extends Block {
	
	public MagicalFocusBlock(AbstractBlock.Properties properties) {
		super(properties);
	}
}
